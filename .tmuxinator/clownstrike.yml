# ~/.tmuxinator/clownstrike.yml
# you can make as many tabs as you wish...

project_name: clownstrike
project_root: ~/Code/mandiant/fenix
windows:
  - editor: vim
  - shell: 
  - shell: 
  - shell: 
  - shell: sudo openconnect vpneng-i.fireeye.com -u timothy.wingfield
    # for regular vpn: sudo openconnect fevpn.fireeye.com -u timothy.wingfield
